# Godot 4 import cache / generated files
.godot/
.import/
export.cfg
export_presets.cfg

# Mono / .NET (unused — GDScript only — but ignore if ever added)
.mono/
data_*/
mono_crash.*.json

# Build artifacts
build/
*.tmp

# OS noise
.DS_Store
Thumbs.db

# Editor noise
*.translation
